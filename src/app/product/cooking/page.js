import React from 'react';
import SliderBanner from '@/layouts/SliderBanner';

const page = () => {
    return (
        <section>
            <SliderBanner/>
        </section>
    );
};

export default page;