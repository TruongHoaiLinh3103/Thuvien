import SliderBanner from '@/layouts/SliderBanner';
import React from 'react';

const page = () => {
    return (
        <section>
            <SliderBanner/>
        </section>
    );
};

export default page;