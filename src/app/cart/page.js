import DetailCart from '@/layouts/DetailCart';
import React from 'react';

const page = () => {
    return (
        <section>
            <DetailCart/>
        </section>
    );
};

export default page;